---
pagination:
    data: collections.posts
    size: 10
    alias: posts
    reverse: true
title: Blog Archive
layout: main/base
---

<h2 class="heading--main">Blog Archive</h2>

{% for post in posts %}
    <h3><a href="{{post.url}}">{{ post.data.title }}</a></h3>
    <p>Posted {{ post.date | niceDate }}</p>
{% endfor %}

<p>
    {% if pagination.href.previous %}
        <a href=" {{ pagination.href.previous }}">Previous</a>
    {% else %}
        Previous
    {% endif %}
    /
    {% if pagination.href.next %}
        <a href=" {{ pagination.href.next }}">Next</a>
    {% else %}
        Next
    {% endif %}
</p>